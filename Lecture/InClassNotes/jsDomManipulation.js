/*Warmup
srevre

or

server


function poke(){
    alert("ouch")

}
setTimeout(poke, 1000);

call poke after 1000ms need to get used to treating functions as data

setInterval(food, 1000);
do the passed function after the given interval repeatedly

Lets work on functional programming
Dont call the function or use anon and arrow functions

Lets talk about the dom
also some event handling

getting one node
document.getElementById("id")
document.querySelector("css selector")
getElements by ClassName, TageName
querySelectorAll
elements and all will return a NodeList which you can loop through

Every element node can use the above document.getElement etc functions
Can also do
node.parentNode
node.childNodes (returns nodeArray )
node.children      (only html children)
node.nextElementSibling
node.previousElementSibling


Element text has written text attributes to change
document.getElement().innerText="stufdibsg"
innerHTML allows you to write html with the inner text like "<strong>hello<strong>"

modifications of the tree
s

someNode.cloneNode()
document.rcreateElement/textNode
parent.removeChild() //parent from node.parent
Adding nodes
at end parent.appendChild(newNode)
or
parent.insettBefore(newNode, sibling)

add operations will just take it from one place and put it where you want it


History 


CLass modifications

elem.classList.add/remove/replace/toggle/toggle(class, expression)







*/
